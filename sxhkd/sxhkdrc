#
# wm independent hotkeys
#

# terminal emulator
super + Return
	kitty

# browser
super + shift + f
	firefox

# program launcher
super + @space
	rofi -no-lazy-grab -show drun -modi drun -theme ~/.cache/wal/colors-rofi-dark.rasi

# make sxhkd reload its configuration files:
super + Escape
	pkill -USR1 -x sxhkd && notify-send 'sxhkd' 'Reloaded config'

#
# bspwm hotkeys
#

# quit/restart bspwm
super + alt + {q,r}
	bspc {quit,wm -r && notify-send 'bspwm' 'Reloaded config'} 

# close and kill
super + {_,shift + }w
	bspc node -{c,k}

# alternate between the tiled and monocle layout
super + m
	bspc desktop -l next

# send the newest marked node to the newest preselected node
super + y
	bspc node newest.marked.local -n newest.!automatic.local

# swap the current node and the biggest window
super + g
	bspc node -s biggest.window

#
# state/flags
#

# set the window state
super + {t,shift + t,s,f}
	bspc node -t {tiled,pseudo_tiled,floating,fullscreen}

# set the node flags
super + ctrl + {m,x,y,z}
	bspc node -g {marked,locked,sticky,private}

#
# focus/swap
#

# focus the node in the given direction
super + {_,shift + }{h,j,k,l}
	bspc node -{f,s} {west,south,north,east}

# focus the node for the given path jump
super + {p,b,comma,period}
	bspc node -f @{parent,brother,first,second}

# focus the next/previous window in the current desktop
super + {_,shift + }c
	bspc node -f {next,prev}.local.!hidden.window

# focus the next/previous desktop in the current monitor
super + bracket{left,right}
	bspc desktop -f {prev,next}.local

# focus the last node/desktop
super + {grave,Tab}
	bspc {node,desktop} -f last

# focus the older or newer node in the focus history
super + {o,i}
	bspc wm -h off; \
	bspc node {older,newer} -f; \
	bspc wm -h on

# focus or send to the given desktop
super + {_,shift + }{1-9,0}
	bspc {desktop -f,node -d} '^{1-9,10}'

#
# preselect
#

# preselect the direction
super + ctrl + {h,j,k,l}
	bspc node -p {west,south,north,east}

# preselect the ratio
super + ctrl + {1-9}
	bspc node -o 0.{1-9}

# cancel the preselection for the focused node
super + ctrl + space
	bspc node -p cancel

# cancel the preselection for the focused desktop
super + ctrl + shift + space
	bspc query -N -d | xargs -I id -n 1 bspc node id -p cancel

#
# move/resize
#

# expand a window by moving one of its side outward
super + alt + {h,j,k,l}
	bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}

# contract a window by moving one of its side inward
super + alt + shift + {h,j,k,l}
	bspc node -z {right -20 0,top 0 20,bottom 0 -20,left 20 0}

# move a floating window
super + {Left,Down,Up,Right}
	bspc node -v {-20 0,0 20,0 -20,20 0}
#
# Media keys
# 

XF86Audio{Lower,Raise}Volume
        pamixer {-d,-i} 5

     
XF86AudioMute
        amixer -q set Master toggle
       
XF86AudioPlay
	/home/noventicx/.config/scripts/playerctl-spotify.sh
    # playerctl play-pause 

XF86Audio{Next,Prev}
    {/home/noventicx/.config/scripts/playerctl-spotify-next.sh,/home/noventicx/.config/scripts/playerctl-spotify-prev.sh}

#
# Lock Screen
#

# lock the screen
super + x
	light-locker-command --lock
#
# maim screenshots
#

# take screenshot of main screen
Print
	maim -g 2560x1440+1024+0 | tee /home/noventicx/Bilder/screenshots/$(date +"%a-%e-%b-%y-%R")-scr1.png | tee /tmp/scr.png | xclip -selection clipboard -t image/png && notify-send "Screenshot is now in clipboard" "Location: ~/Bilder/screenshots \nName: $(date +"%a-%e-%b-%y-%R")-scr1.png" -i /tmp/scr.png

# take screenshot of 2. screen
super + Print
	maim -g 1280x1024+3584+160 | tee /home/noventicx/Bilder/screenshots/$(date +"%a-%e-%y-%b-%R")-scr2.png | tee /tmp/scr.png | xclip -selection clipboard -t image/png && notify-send "Screenshot is now in clipboard" "Location: ~/Bilder/screenshots \nName: $(date +"%a-%e-%b-%y-%R")-scr2.png" -i /tmp/scr.png

# take screenshot of 3. screen
super + shift + Print
	maim -g 1024x768+0+336 | tee /home/noventicx/Bilder/screenshots/$(date +"%a-%e-%b-%y-%R")-scr3.png | tee /tmp/scr.png | xclip -selection clipboard -t image/png notify-send "Screenshot is now in clipboard" "Location: ~/Bilder/screenshots \nName: $(date +"%a-%e-%b-%y-%R")-scr3.png" -i /tmp/scr.png

# take screenshot of all screens
alt + Print
	maim | tee /home/noventicx/Bilder/screenshots/$(date +"%a-%e-%b-%y-%R")-scr1-3.png | tee /tmp/scr.png | xclip -selection clipboard -t image/png && notify-send "Screenshot is now in clipboard" "Location: ~/Bilder/screenshots \nName: $(date +"%a-%e-%b-%y-%R")-scr1-3.png" -i /tmp/scr.png

# take screenshot of selected area
super + shift + s
	maim --select | tee /home/noventicx/Bilder/screenshots/$(date +"%a-%e-%b-%y-%R")-sl.png | tee /tmp/scr.png | xclip -selection clipboard -t image/png && notify-send "Screenshot is now in clipboard" "Location: ~/Bilder/screenshots \nName: $(date +"%a-%e-%b-%y-%R")-sl.png" -i /tmp/scr.png

#
# Show Help
#

# show help
super + shift + @space
	/home/noventicx/.config/scripts/sxhkd-help

#
# Look up definition
#

# get definition from selected word
super + d
	/home/noventicx/.config/scripts/word-lookup.sh
